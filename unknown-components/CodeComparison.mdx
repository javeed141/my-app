{/* Unknown component: <CodeComparison> */}

export const CodeComparison = ({ before, after, language }) => (
  <div className="grid grid-cols-2 gap-4 my-4">
    <div>
      <div className="bg-red-100 text-red-800 px-3 py-1 text-sm font-bold rounded-t">❌ Before</div>
      <pre className="bg-gray-900 text-gray-100 p-4 rounded-b"><code>{before}</code></pre>
    </div>
    <div>
      <div className="bg-green-100 text-green-800 px-3 py-1 text-sm font-bold rounded-t">✅ After</div>
      <pre className="bg-gray-900 text-gray-100 p-4 rounded-b"><code>{after}</code></pre>
    </div>
  </div>
);

<CodeComparison 
  before="const client = new Acme({ token: 'xxx' });"
  after="const client = new AcmeClient({ apiKey: 'xxx', version: 'v2' });"
  language="javascript"
/>

<CodeComparison 
  before="client.get('/users', callback);"
  after="const users = await client.users.list();"
  language="javascript"
/>
