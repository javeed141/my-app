{/* Unknown component: <CertBadge> */}

export const CertBadge = ({ name, logo, validUntil, reportUrl }) => (
  <div className="flex items-center gap-4 border rounded-lg p-4 my-3">
    <img src={logo} alt={name} className="w-16 h-16 object-contain" />
    <div className="flex-1">
      <div className="font-bold">{name}</div>
      <div className="text-sm text-gray-500">Valid until {validUntil}</div>
    </div>
    {reportUrl && (
      <a href={reportUrl} className="text-blue-500 text-sm font-medium">View Report →</a>
    )}
  </div>
);

<CertBadge
  name="SOC 2 Type II"
  logo="https://placehold.co/100x100/1e40af/white?text=SOC2"
  validUntil="March 2026"
  reportUrl="/security/soc2-report"
/>

<CertBadge
  name="PCI DSS Level 1"
  logo="https://placehold.co/100x100/7c3aed/white?text=PCI"
  validUntil="December 2025"
  reportUrl="/security/pci-report"
/>

<CertBadge
  name="ISO 27001"
  logo="https://placehold.co/100x100/059669/white?text=ISO"
  validUntil="June 2026"
  reportUrl="/security/iso27001"
/>

<CertBadge
  name="GDPR Compliant"
  logo="https://placehold.co/100x100/dc2626/white?text=GDPR"
  validUntil="Ongoing"
/>
