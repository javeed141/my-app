{/* Unknown component:  */}

export const CertBadge = (`{ name, logo, validUntil, reportUrl }`) => (
  <div>
    <img src={logo} alt={name} />
    <div>
      <div>`{name}`</div>
      <div>Valid until `{validUntil}`</div>
    </div>
    `{reportUrl && (
      <a href={reportUrl}`>View Report →</a>
    )}
  </div>
);








