# Originate a Real-Time Payment (RTP)

Real-Time Payments can be originated from any account with the corresponding capability. To initiate an RTP, simply set the `type` to `rtp` and direction to `credit`.

See [Payment Order Examples](https://docs.moderntreasury.com/reference/create-payment-order-examples#rtp-credit) for a detailed RTP example.