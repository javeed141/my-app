{/* Unknown component:  */}

export const EnvironmentSwitcher = (`{ environments }`) => `{
  const [active, setActive] = useState(0);
  const env = environments[active];
  return (
    <div>
      <div>
        {environments.map((e, i) => (
          <button
            key={i}`
            onClick={() => setActive(i)}`}
          >
            `{e.name}`
          </button>
        ))}
      </div>
      <div>
        <div>`{env.baseUrl}`</div>
        <p>`{env.description}`</p>
        <div>API Key prefix: <code>`{env.keyPrefix}`</code></div>
      </div>
    </div>
  );
};


