{/* Unknown component: <CodePlayground> */}

export const CodePlayground = ({ title, language, code, output, runnable }) => {
  const [showOutput, setShowOutput] = useState(false);
  return (
    <div className="border rounded-lg overflow-hidden my-6">
      <div className="bg-gray-800 text-white px-4 py-2 flex justify-between items-center">
        <span className="font-mono text-sm">{title}</span>
        {runnable && (
          <button onClick={() => setShowOutput(!showOutput)} className="bg-green-500 text-white px-3 py-1 rounded text-sm">
            {showOutput ? 'Hide Output' : '▶ Run'}
          </button>
        )}
      </div>
      <pre className="p-4 bg-gray-900 text-green-400 overflow-x-auto m-0">
        <code>{code}</code>
      </pre>
      {showOutput && output && (
        <div className="bg-black text-white p-4 border-t border-gray-700">
          <div className="text-xs text-gray-400 mb-1">Output:</div>
          <pre className="m-0 text-sm">{output}</pre>
        </div>
      )}
    </div>
  );
};

<CodePlayground
  title="create-payment.js"
  language="javascript"
  code={`const payment = await client.payments.create({
  amount: 5000,
  currency: 'usd',
  customer: 'cus_abc123',
  payment_method: 'pm_card_visa',
  confirm: true
});

console.log(payment.status);`}
  output="succeeded"
  runnable={true}
/>

<CodePlayground
  title="list-transactions.py"
  language="python"
  code={`import acme

client = acme.Client(api_key="sk_test_...")

transactions = client.transactions.list(
    limit=10,
    status="completed"
)

for txn in transactions:
    print(f"{txn.id}: {txn.amount/100:.2f} {txn.currency.upper()}")`}
  output={`txn_1a2b3c: 50.00 USD
txn_4d5e6f: 125.00 USD
txn_7g8h9i: 30.00 EUR`}
  runnable={true}
/>
