{/* Unknown component:  */}

export const VersionTimeline = (`{ versions }`) => (
  <div>
    `{versions.map((v, i) => (
      <div key={i}`>
        <div />
        <div>`{v.version}`</div>
        <div>`{v.date}`</div>
        <ul>
          `{v.changes.map((c, j) => <li key={j}`>`{c}`</li>)}
        </ul>
      </div>
    ))}
  </div>
);


