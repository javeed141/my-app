{/* Unknown component:  */}

export const LogoGrid = (`{ title, logos }`) => (
  <div>
    <h3>`{title}`</h3>
    <div>
      `{logos.map((logo, i) => (
        <a key={i}` href={logo.url} title={logo.name}>
          <img src={logo.src} alt={logo.name} />
        </a>
      ))}
    </div>
  </div>
);


