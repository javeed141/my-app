{/* Unknown component:  */}

export const TeamShowcase = (`{ members }`) => (
  <div>
    `{members.map((m, i) => (
      <div key={i}`>
        <img src={m.avatar} alt={m.name} />
        <h4>`{m.name}`</h4>
        <p>`{m.role}`</p>
        <a href={m.github}>GitHub →</a>
      </div>
    ))}
  </div>
);


