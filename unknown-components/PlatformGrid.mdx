{/* Unknown component:  */}

export const PlatformGrid = (`{ platforms }`) => (
  <div>
    `{platforms.map(p => (
      <div key={p.name}`>
        <img src={p.logo} alt={p.name} />
        <span>`{p.name}`</span>
        <span`}>
          `{p.status}`
        </span>
      </div>
    ))}
  </div>
);


