{/* Unknown component: <StatusBadge> */}

export const StatusBadge = ({ status, service }) => {
  const colors = { operational: 'green', degraded: 'yellow', outage: 'red' };
  return (
    <span className={`inline-flex items-center gap-1 px-2 py-1 rounded text-sm bg-${colors[status]}-100 text-${colors[status]}-800`}>
      <span className={`w-2 h-2 rounded-full bg-${colors[status]}-500`} />
      {service}: {status}
    </span>
  );
};

<StatusBadge status="operational" service="API Gateway" />

<StatusBadge status="operational" service="Authentication" />

<StatusBadge status="degraded" service="Webhooks" />
