{/* Unknown component: <FeatureHighlight> */}

export const FeatureHighlight = ({ title, description, icon, badge }) => (
  <div className="feature-card p-6 rounded-xl shadow-md border border-gray-200 my-4">
    <div className="flex items-center gap-3 mb-2">
      <i className={`fa-${icon} text-2xl text-blue-500`} />
      <h3 className="text-lg font-bold">{title}</h3>
      {badge && <span className="bg-green-100 text-green-800 text-xs px-2 py-1 rounded-full">{badge}</span>}
    </div>
    <p className="text-gray-600">{description}</p>
  </div>
);

<FeatureHighlight 
  title="Real-time Sync" 
  description="Automatically sync data across all connected clients with sub-100ms latency."
  icon="bolt"
  badge="NEW"
/>

<FeatureHighlight 
  title="End-to-End Encryption" 
  description="All data is encrypted at rest and in transit using AES-256."
  icon="shield"
  badge="Enterprise"
/>

<FeatureHighlight 
  title="Webhooks" 
  description="Get instant notifications when events happen in your account."
  icon="bell"
/>
