{/* Unknown component:  */}

export const RateLimitTable = (`{ limits }`) => (
  <div>
    <table>
      <thead>
        <tr>
          <th>Endpoint</th>
          <th>Free</th>
          <th>Pro</th>
          <th>Enterprise</th>
        </tr>
      </thead>
      <tbody>
        `{limits.map((l, i) => (
          <tr key={i}`>
            <td>`{l.endpoint}`</td>
            <td>`{l.free}`</td>
            <td>`{l.pro}`</td>
            <td>`{l.enterprise}`</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
);


