{/* Unknown component:  */}

export const Terminal = (`{ commands }`) => `{
  return (
    <div>
      <div>
        <span />
        <span />
        <span />
      </div>
      {commands.map((cmd, i) => (
        <div key={i}`>$ `{cmd}`</div>
      ))}
    </div>
  );
};


