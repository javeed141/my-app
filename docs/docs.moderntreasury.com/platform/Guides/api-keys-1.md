# API Keys

When you make API calls, you will include an API key as part of [basic authentication](https://docs.moderntreasury.com/platform/reference/authentication). Each API key can only interact with resources in its environment. Additionally, you can configure API keys to have more limited access to resources.

Every Modern Treasury organization has an API key in both production and sandbox by default. These API keys have full permissions to data in their respective environments.