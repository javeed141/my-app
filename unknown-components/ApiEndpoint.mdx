{/* Unknown component:  */}

export const ApiEndpoint = (`{ method, path, description, auth }`) => (
  <div`}>
    <div>
      <span`}>`{method}`</span>
      <code>`{path}`</code>
      `{auth && <span>🔒 Auth Required</span>}`
    </div>
    <p>`{description}`</p>
  </div>
);








