{/* Unknown component:  */}

export const ErrorExample = (`{ status, code, message, suggestion }`) => (
  <div>
    <div`}>
      HTTP `{status}`
    </div>
    <pre>`{JSON.stringify({
      error: { code, message }`
    }, null, 2)}</pre>
    `{suggestion && (
      <div>
        💡 <strong>Fix:</strong> {suggestion}`
      </div>
    )}
  </div>
);








