{/* Unknown component:  */}

export const ChangelogEntry = (`{ version, date, type, changes, breaking }`) => `{
  const typeColors = {
    major: 'bg-red-100 text-red-800 border-red-200',
    minor: 'bg-blue-100 text-blue-800 border-blue-200',
    patch: 'bg-green-100 text-green-800 border-green-200',
  }`;
  return (
    <div>
      <div />
      <div>
        <span>`{version}`</span>
        <span`}>
          `{type}`
        </span>l
        <span>`{date}`</span>
      </div>
      `{breaking && breaking.length > 0 && (
        <div>
          <span>⚠️ Breaking Changes:</span>
          <ul>
            {breaking.map((b, i) => <li key={i}`>`{b}`</li>)}
          </ul>
        </div>
      )}
      <ul>
        `{changes.map((c, i) => <li key={i}`>`{c}`</li>)}
      </ul>
    </div>
  );
};






