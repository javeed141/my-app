# Injectors

Inject code directly into Generators.

[block:callout]
{
  "type": "warning",
  "body": "This is an advanced option and should only be used if no other element solves a problem."
}
[/block]

Injectors inject code into your Generator templates. These injectors are added prior to code generation, so they will go through any elements that are used by that Generator.