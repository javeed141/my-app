{/* Unknown component:  */}

export const DiagramCard = (`{ title, imageSrc, caption, width }`) => (
  <figure>
    <div>
      <img src={imageSrc} alt={title} width={width || 800} />
    </div>
    <figcaption>`{caption}`</figcaption>
  </figure>
);




