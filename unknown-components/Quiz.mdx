{/* Unknown component:  */}

export const Quiz = (`{ question, options, correct }`) => `{
  const [selected, setSelected] = useState(null);
  return (
    <div>
      <p>{question}`</p>
      `{options.map((opt, i) => (
        <button key={i}` onClick={() => setSelected(i)}`}>
          `{opt}`
        </button>
      ))}
      `{selected !== null && selected === correct && (
        <p>✅ Correct!</p>
      )}`
      `{selected !== null && selected !== correct && (
        <p>❌ Try again. The correct answer is: {options[correct]}`</p>
      )}
    </div>
  );
};




